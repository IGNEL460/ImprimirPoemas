# Reglas del Proyecto (ImprimirPoemas)

## Control de Cambios y Git
- **Suspendidos los push automáticos a GitHub**: Queda estrictamente prohibido realizar `git push` o enviar cambios de forma automática al repositorio remoto en GitHub.
- Los `git push` solo deben ejecutarse cuando el usuario lo solicite de manera explícita en la conversación.
